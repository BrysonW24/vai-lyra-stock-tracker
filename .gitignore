node_modules/
.next/
.next-verify/
out/
coverage/
# Per-clone commissioning receipts - private local keepsakes, never committed.
/commission/
/COMMISSIONED.md
.env
.env.local
.env.*.local
.venv/
__pycache__/
.pytest_cache/
*.pyc
*.tsbuildinfo
tmp/
*.sqlite
*.duckdb
.fuse_hidden*
.vercel

# Local MCP tooling config - project-specific, not part of the shareable app
.mcp.json

# Sentry Config File
.env.sentry-build-plugin

# Corrupt/interrupted build output - regenerable, never tracked
.next.stale-corrupt/
.next-*/

# Emerging Winner real-history backtest cache (prices + EDGAR bundles + corpus) - fetched, never tracked
.ew-backtest-cache/
