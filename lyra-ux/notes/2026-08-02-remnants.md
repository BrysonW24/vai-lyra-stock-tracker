# Remnants - orphan-candidate cleanup (2026-08-02)

Final cleanup package of the app-wide token convergence: the three files the inventory flagged
as orphan candidates. Contract: lyra-ux/TOKENS.md v1.0.0 + lyra-ux/PATTERNS.md, following the
P0/P1/P2/P8 nearest-token rulings. Verified: zero raw hex / white- or black-opacity literals /
off-token radii remain in the three files (grep scan clean), `npx tsc --noEmit` clean,
`node lyra-ux/check-tokens.mjs` OK. No exports, props, behaviour, or copy changed. No commits.

## Orphan verdicts (checked, not assumed)

The inventory's "no importers found" claim is WRONG for two of the three files. Verified by
grepping for component names and path strings repo-wide; the codebase contains zero `dynamic()`
or string-based component imports, so static imports are the whole truth.

| File | Verdict | Evidence |
|---|---|---|
| `src/components/LandingShowcase.tsx` | **ORPHAN CONFIRMED** | No code importers anywhere. Only mention outside the file is prose in `docs/product/mobile-experience.md`. Safe to remount later; now token-clean. |
| `src/components/FeedbackWidget.tsx` | **ORPHAN REFUTED - LIVE** | Imported by `src/components/DualLauncher.tsx` (line 6, mounted line 107) -> `AppShell.tsx` (line 584) -> every AppShell route (/, /saved, /settings, /fundamentals, ...). |
| `src/components/chat/ChatWidget.tsx` | **ORPHAN REFUTED - LIVE** | Imported by `src/components/DualLauncher.tsx` (line 5, mounted line 106) -> same AppShell chain. Also name-checked in `src/lib/focus-trap.ts` doc comment. |

Likely explanation: DualLauncher was created/wired by P0 (its note restyles DualLauncher) after
the inventory sweep ran. Consequence: FeedbackWidget and ChatWidget changes are user-visible on
every shell route, not dormant. Their restyle stays value-faithful for exactly that reason.

## What changed

### LandingShowcase.tsx (~40 class/value swaps)

- All canonical hexes -> token names (`#eef3f8` ink, `#dbe5ee` ink-title, `#a8b5c2` ink-2,
  `#8190a0` ink-3, `#f3a33a` accent, `#43d18b` positive, `#60a5fa` blue-focus - all byte-exact).
- Ruling-table hexes: `#5fd08a` -> `positive`, `#cdd8e3` -> `ink-title`, `#6f7d8a` -> `ink-dim`
  (P1/P2); `#1f5132` border + `#0f2417` fill -> `positive/40` + `positive-tint` (P1 green sets);
  `#161e27` breakdown track -> `line/70` (P2 track ruling for the near-dupe `#17202a`).
- White-opacity dialect: root glass card `border-white/10 bg-white/[0.03] rounded-2xl` ->
  `border-line-strong bg-panel/40 rounded-panel` (P1 card-border ruling; opacity kept per the
  P8 `bg-[#0c1118]/90 -> bg-panel-deep/90` keep-translucency precedent, so the hero glass +
  backdrop-blur-xl reading survives). Stat cells `bg-white/[0.04] border-white/10 rounded-lg` ->
  `bg-well border-line rounded-cell` per the PATTERNS StatCell shape (inert `backdrop-blur`
  dropped with the now-opaque fill). Sheen hairline `via-white/30` -> `via-ink/30` (P8 ruling).
  Header/footer dividers `border-white/10` -> `border-line` (P1 divider ruling). Inactive slide
  dot `bg-white/20` -> `bg-line-hair` (P0 DualLauncher dot ruling). Amber glow `bg-[#f3a33a]/10`
  -> `bg-accent/10`.
- NUGGET_META refactor: `color: '#hex'` + `style={{ color }}` -> `colorClass` Tailwind names
  (module-private record; no export/prop change). Mappings: fact `#60a5fa` -> `text-blue-focus`
  (byte-exact), tip `#43d18b` -> `text-positive`, think `#f3a33a` -> `text-accent`, book
  `#a78bfa` -> `text-pending` (P2 violet ruling), market `#4fd1d9` -> `text-blue-info`.
- **New ruling: teal `#4fd1d9` -> `blue-info`.** No teal token exists; P0's NAV_BUCKETS ruling
  (cyan -> blue-info) is the precedent, and "the bigger picture" market context is info-text
  semantics. Recorded here for any future teal stragglers.
- Kept: `shadow-[...rgba(0,0,0,0.8)]` (black shadow alphas are not palette values - P0 accepted
  literal class, P1 left the same shadow on FeedbackWidget); tiny "Buy · review" chip keeps
  plain `rounded` (P1 tiny-chip precedent); progress bars keep `rounded-full`.

### FeedbackWidget.tsx (already mostly converged by an earlier pass)

- `border-[#f3a33a]` x2 (active type chip, send button) -> `border-accent` (byte-exact).
- Standalone launcher pill `border-white/10 bg-white/[0.06]` -> `border-line bg-chrome/80`,
  matching P0's DualLauncher pill ruling so the two floating launchers read as one family.
- Sheet `rounded-2xl border-white/10` -> `rounded-panel border-line-strong`; header divider ->
  `border-line`; sheen `via-white/25` -> `via-ink/25` (P8).
- Radius convergence: type chips / Ideas-board link / textarea / email input / checkbox row /
  send button `rounded-md` -> `rounded-cell` (P1/P2 buttons-and-inputs precedent).
- Copy untouched, including the Ideas-board routing copy and the "we got it" state.

### chat/ChatWidget.tsx

- Every `#8aa2ff` (13 hex literals across ~19 class occurrences) -> `pending` with the same
  opacity suffixes (`border-pending/25|30|40`, `text-pending`, bare `border-pending`).
  Byte-exact: `#8aa2ff` IS the pending token, and P0 already ruled the Ask Lyra identity =
  pending violet in DualLauncher. The pre-existing violet-border + `bg-blue-tint` fill pairing
  is kept per P1's "pending-context fills -> blue-tint (no pending-tint token)" ruling.
- Whites: sheet `border-white/10` -> `border-line-strong` (card); header + composer dividers ->
  `border-line`.
- Radius convergence: sheet `rounded-2xl` -> `rounded-panel`; icon tile / New-chat button /
  saved-prompt rows / question rows / suggestion rows / action card / confirm button / AI
  settings link `rounded-lg|md` -> `rounded-cell`; composer textarea + send button `rounded-xl`
  -> `rounded-cell` (P1 inner-element precedent); qty/price inline inputs `rounded` ->
  `rounded-cell` (PATTERNS: inputs are cells; they already carry min-h-[44px]). Tiny undo chip
  keeps plain `rounded` and category chips keep `rounded-full` (chip radius) per precedent.
- **New ruling: chat message bubbles `rounded-2xl rounded-b{l,r}-sm` -> `rounded-panel` +
  unchanged small-corner cut.** Nearest token radius; `rounded-cell` would flatten the bubble
  identity. Applies to user/assistant bubbles and the "Lyra is reading" bubble.

## Honesty/provenance copy verified intact (verbatim)

- LandingShowcase: the real-product-not-mockup doc comment; "Live market data from Yahoo
  Finance · chart by TradingView." attribution footer; the daily-brief slide copy.
- ChatWidget: "AI copilot · grounded in your dashboard"; "I'll only answer from what's on your
  dashboard"; "You confirm; Lyra never acts on its own. Reversible - you can undo it.";
  "AI-generated answers - research, not financial advice. Lyra answers only from your data.";
  the on-this-device vs cash-and-holdings trade-log distinction; demo-mode sign-in copy; the
  connect-a-model / hosted-key states and their explanatory copy.
- FeedbackWidget: all copy unchanged.

## Deferred / observations

- ChatWidget category chips stay violet-active (pending tokens) rather than converting to the
  amber ChoiceChip pattern - a semantic redesign beyond token convergence; same deferral P2
  recorded for PanelCarousel's colour-coded tabs.
- NUGGET_META colour-codes categories with status colours (kept value-faithful) - same tension
  P0 flagged for NAV_BUCKETS/LYRA_RAMP; flatten only as a deliberate design decision.
- Small targets left as-is (pre-existing, layout-coupled): LandingShowcase slide dots (h-1.5),
  FeedbackWidget/ChatWidget header close buttons (icon-sized), ChatWidget New-chat button
  (py-1). Composer send button and qty/price inputs already meet 44px.
- FeedbackWidget send button carries a no-op `hover:bg-accent-tint` over a `bg-accent-tint`
  base (pre-existing); left verbatim - removing it is cleanup beyond this scope.
- The restyle-inventory orphan line should be treated as superseded by the verdicts above;
  InvestigationDrawerStack and FindingCard were not in this package and remain unverified here
  (HoldingChartCarousel was already dispositioned by P2).
