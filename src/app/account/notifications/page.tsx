import { AccountSettings } from '@/components/AccountSettings';

export const metadata = { title: 'Notifications' };

export default function NotificationsSettingsPage() {
  return <AccountSettings section="notifications" />;
}
