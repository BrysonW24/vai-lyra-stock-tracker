import { AccountSettings } from '@/components/AccountSettings';

export const metadata = { title: 'AI Settings' };

export default function AiSettingsPage() {
  return <AccountSettings section="ai" />;
}
