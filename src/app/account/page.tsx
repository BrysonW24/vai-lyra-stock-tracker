import { AccountSettings } from '@/components/AccountSettings';

export const metadata = { title: 'Account' };

export default function AccountPage() {
  return <AccountSettings section="account" />;
}
